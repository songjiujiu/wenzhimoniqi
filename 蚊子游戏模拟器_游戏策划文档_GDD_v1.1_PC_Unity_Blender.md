# 蚊子游戏模拟器 · 游戏策划文档 GDD v1.1

**技术栈：Unity + C# + Blender｜目标：PC 单机原型｜日期：2026-09-20**

| 项目 | 内容 |
|---|---|
| 文档状态 | 原型开发建议基线；新增产品选择与性能指标需要原型验证 |
| 原始依据 | 《蚊子游戏模拟器_游戏策划文档_GDD_v1.0.md》及其缺口分析 |
| 用户已明确的范围 | 使用 Unity + Blender；目前仅开发 PC 端 |
| 本版建议的产品基线 | Windows x64、单机、本地存档、固定镜头 3D 场景、全局攻击、无限沙盒 |
| 核心体验 | 看蚊群增长，使用武器清除，观察幸存虫卵重新孵化，刷新击杀纪录 |
| 原型目的 | 验证攻击反馈、清场回弹和全武器解锁后的重复操作意愿 |

> 本版完整替代 v1.0 作为建议开发规格阅读，原文件保留。Windows、镜头、操作方式、存档政策等属于本次补全提出的方案，不表示用户已经逐项确认。文中的“规则”用于保证本版内部一致；需要改变时应更新本版相关章节、配置与验收用例，而不是让实现各自解释。

## 目录

1. 版本变化与范围
2. 游戏定位、目标与核心循环
3. PC 镜头与操作
4. 繁殖、性别与数量规则
5. 武器与解锁
6. 蚊香清场与虫卵回弹
7. 状态机、时间与事件顺序
8. 数值基线与首次体验
9. UI、提示与数字表达
10. Unity 技术设计
11. Blender 资产与制作规范
12. 视觉、动画与音频
13. 存档、暂停与本地纪录
14. 性能目标与降级
15. MVP 范围与实施里程碑
16. 验收测试与试玩评估
17. 后续扩展与待验证事项
18. 附录：配置、统计、伪代码与参考资料

## 1. 版本变化与范围

### 1.1 保留的核心内容

- 初始 2 只公蚊、2 只母蚊、0 枚虫卵。
- 母蚊每 1 秒产 1 枚虫卵，虫卵在生成 0.5 秒后孵化。
- 后代性别以公母各半为目标；大批量分配的近似方式在第 4.3 节明确。
- 手掌在 10 只解锁，50% 命中，成功消灭 1 只。
- 电蚊拍一次最多消灭 10 只；蚊香清除所有成蚊，不清除虫卵。
- 武器由本局历史最高成蚊数量解锁，数量下降不重新上锁。
- 不按逻辑总量创建实体对象，逻辑数量与可见数量分离。

### 1.2 对 v1.0 的修订

| 问题 | v1.1 处理 |
|---|---|
| 数量纪录与消灭蚊子的动机冲突 | 主模式明确为沙盒；攻击纪录以本局累计击杀和单次清场规模为主，数量峰值用于进度与观察 |
| 没有危险却统计“存活时长” | 改称“本局模拟时长”，不作为生存成绩 |
| 成蚊、虫卵清零后无后续事件 | 增加“繁殖结束”结算，包括只剩公蚊的情况 |
| 虫卵孵化与安静真空期冲突 | 保留原孵化时点；取消必须静默 0.1–0.2 秒的要求 |
| 普通 0.5 秒孵化被描述为必然加速爆发 | 明确为幸存虫卵回弹；不保证每次有虫卵，也不凭空增加数量 |
| 时序各章不一致 | 固定为同 Tick 先攻击、再产卵、再孵化 |
| 失焦暂停与后台补算冲突 | 首版统一暂停，无后台及离线增长 |
| 单体与聚合切换可能改变规则 | 从开局就使用统一聚合逻辑，低数量仅在表现上对应单只 |
| “无限”和精度没有边界 | 不设玩法数量上限；使用精确整数计数，并明确测试覆盖范围 |
| PC 交互与美术交付缺失 | 补齐操作表、固定镜头、资产清单、FBX 交付和验收标准 |

### 1.3 首版产品边界

建议首版交付 Windows 11 x64 可执行程序，使用键盘与鼠标。其他 Windows 版本是否支持，以选定 Unity 版本要求和实际测试为准；本版不承诺 macOS、Linux 或手柄支持。

首版包括一个室内场景、三种武器、基础设置、本地继续游戏、纪录与繁殖结束结算。联网、排行榜、商店、多人、第一人称移动、多地图、升级树及离线收益均不进入 MVP。Steam 上架集成属于后续发行任务，不是本原型的完成条件。

## 2. 游戏定位、目标与核心循环

### 2.1 一句话定位

在一个固定镜头的房间里，从四只蚊子开始观察指数增长，选择清除时机，用逐级解锁的武器制造“清场—幸存虫卵回弹”的反转，并记录自己的清除规模。

### 2.2 玩家目标

| 层级 | 目标 | 玩家行为 |
|---|---|---|
| 即时 | 获得明确的拍击、电击和清场反馈 | 使用当前武器，观察命中与击杀数字 |
| 短期 | 解锁三种武器、理解母蚊与虫卵的作用 | 等待增长，比较攻击前后的繁殖速度 |
| 重复体验 | 提高本局累计击杀、单次蚊香击杀纪录 | 选择更大的清场时机，观察回弹后再操作 |
| 观察 | 达到更大数量等级 | 查看峰值、数量层级与场景变化 |

本模式没有蚊群过多导致的死亡或强制失败，玩家可以只观察。纪录不是公平竞技评分；等待更久可以获得更大清场规模，这是当前沙盒的特点，不包装成复杂策略。若试玩表明缺少操作动机，再单独验证限时挑战，避免提前堆叠成长系统。

### 2.3 循环与局次

核心循环：**繁殖 → 解锁 → 攻击 → 清场 → 幸存虫卵孵化 → 再繁殖。**

局次有三种去向：

- 继续运行：仍有母蚊，或仍有等待孵化的虫卵。
- 繁殖结束：母蚊与虫卵均为零，显示本局结果，可以重新开始或返回主菜单。
- 玩家主动重开：保存历史纪录后，以初始四只开启新局；原局的继续进度被替换。

不会偷偷补入母蚊、隐藏虫卵或场外蚊子来保证循环。后续若增加“外来蚊群”，必须作为玩家可见的新规则。

## 3. PC 镜头与操作

### 3.1 场景与镜头

采用固定透视镜头，展示一个风格化卧室角落。镜头不移动，玩家不控制角色行走。蚊群主要在预设可视空间内活动，武器在前景表现。

首版不做基于真实大小的蚊子：模型尺寸应服务于 1080p 下的辨识度。场景采用明暗对比明确的背景，避免深色蚊子长期消失在纹理中。

### 3.2 操作表

| 输入 | 行为 |
|---|---|
| 数字键 1 / 2 / 3 | 选择手掌 / 电蚊拍 / 蚊香；未解锁时只提示条件，不改变当前选择 |
| 点击武器卡片 | 选择武器，不同时发动攻击 |
| 在场景活动区单击鼠标左键 | 发动已选武器的全局攻击 |
| 按住场景内鼠标左键 | 手掌、电蚊拍按各自冷却自动重复；蚊香仅响应按下瞬间 |
| Space | 发动已选武器一次；忽略系统按键重复 |
| Esc | 打开暂停菜单；由菜单“继续”按钮恢复 |
| 设置页面 | 调节音量、显示和效果；游戏保持暂停 |

第一把武器解锁时自动选中；后续解锁不替玩家切换。新局在手掌解锁前无可用武器，但可观察繁殖。

### 3.3 输入与反馈规则

- 所有攻击均为全局逻辑效果，鼠标位置不改变命中率、范围或目标性别。界面提示“点击场景使用武器”，不要求瞄准具体蚊子。
- 点击 UI 不穿透场景；暂停、设置和结算界面阻断攻击输入。
- 鼠标移出活动区、切换武器、失焦或打开菜单后，停止按住重复，必须释放并重新按下才能恢复。
- 冷却中、未解锁、无成蚊或状态禁止时，攻击无效，不消耗冷却，不排队等待执行。
- 每个模拟 Tick 最多接收一条攻击命令，按输入序号取第一条，其余丢弃。命令携带输入时选定的武器 ID。
- 武器冷却独立；切换不重置也不缩短冷却，允许在相邻 Tick 使用不同武器。
- 50% MISS 只指手掌的规则随机失败，与玩家是否点击到模型无关。

## 4. 繁殖、性别与数量规则

### 4.1 成蚊定义

公蚊与母蚊都是成蚊，都可被武器消灭。只有母蚊产卵。首版不模拟交配、吸血、饥饿、寿命、疾病或自然死亡；公蚊是否存在不影响母蚊产卵。

初始母蚊在模拟时间 1.0 秒第一次产卵。新生母蚊在出生满 1.0 秒第一次产卵，之后每隔 1.0 秒产卵一次。未到首次产卵时点的母蚊仍计入成蚊与母蚊总数，也可以被击杀。

### 4.2 虫卵定义

每次产卵生成 1 枚虫卵；每枚虫卵在生成 0.5 秒后孵化。已生成虫卵与母蚊解绑，击杀母蚊不会删除其既有虫卵。

虫卵不是长期储备：普通情况下只保留最近半秒内尚未孵化的部分。首版没有幼虫、蛹等中间阶段，游戏中的“虫卵”是简化机制。

每次孵化必须满足：`新增公蚊 + 新增母蚊 = 本次孵化虫卵数`。孵化后相同数量从虫卵中移除，不允许同一批虫卵在普通队列与回弹队列重复结算。

### 4.3 性别分配

本版使用明确、可复现且计算量有界的混合方案：

- 单 Tick 总孵化量不超过 1,024 时，对每枚虫卵执行独立的 50% 母蚊随机判定。
- 超过 1,024 时，公母各分配整数的一半；若总数为奇数，剩余 1 只按 50% 随机分配。
- 先合并同 Tick 的所有到期虫卵，再进行一次性别分配，避免内部拆桶改变结果。
- 大批量方案保留公母各半的期望与准确总量，但不保留逐只独立随机的方差。这是本版采用的统计近似，不应称为精确二项分布抽样。
- 使用可保存状态的逻辑随机数发生器；视觉抖动、音效变体不能消耗同一个随机数流。

未来若需要大群体的性别比例波动，应独立增加经过验证的批量抽样算法，并更新数值与回放版本。

### 4.4 繁殖结束

在每个 Tick 的全部攻击、产卵和孵化事件处理完成后，检查：

`femaleCount == 0 && eggCount == 0`

满足时进入 `ReproductionEnded`，停止模拟时钟与攻击，展示结算。剩余公蚊可以继续播放装饰性飞行动画，但数量不再变化。结算分为：

- 成蚊也为 0：“已清除全部蚊子。”
- 仍有公蚊：“繁殖已停止，剩余 X 只公蚊。”

只要仍有虫卵，即使当前没有母蚊，也不能提前结束。最后一批虫卵若全部孵化为公蚊，应在该 Tick 结束后正确结算。

## 5. 武器与解锁

### 5.1 首版参数

| 武器 | 本局成蚊峰值阈值 | 效果 | 命中 | 冷却 | 消耗 |
|---|---:|---|---:|---:|---|
| 手掌 | 10 | 成功消灭 1 只 | 50% | 0.30 秒 / 6 Tick | 无 |
| 电蚊拍 | 100 | 消灭 min(10, 当前成蚊数) | 100% | 0.80 秒 / 16 Tick | 无 |
| 蚊香 | 1,000 | 清除当前全部成蚊，保留虫卵 | 100% | 10.0 秒 / 200 Tick | 无 |

阈值 100、1,000 及冷却是可调基线。手掌 10 只解锁、50% 命中、单次 1 只和电蚊拍最多 10 只，沿用原设计进行首轮验证。

### 5.2 解锁与命中

每次合法数量事件结束后，立即更新本局峰值与解锁。武器一旦解锁，本局内始终可用；继续同一存档保留解锁，重新开始则重新按阈值解锁。跨局历史峰值不会直接解锁新局武器。

手掌只有在通过状态、数量和冷却检查后才消耗冷却并进行命中判定。MISS 消耗冷却并记一次有效尝试，但击杀数为 0。无成蚊时不判定随机命中。

电蚊拍按不放回方式随机消灭目标，公母被选中概率与当前群体数量一致。每次最多只有 10 个逻辑目标，可以逐次抽取，无需按总成蚊数量循环。

### 5.3 击杀如何影响母蚊批次

消灭一只母蚊时，必须同时从母蚊数量及某个待产卵批次扣除 1。该批次按批次数量加权随机选择，不能只减少总数而留下未来产卵事件。

蚊香直接清空所有母蚊待产卵批次。已有虫卵时间桶保持原样。

### 5.4 纪录

成功消灭多少成蚊，就向本局累计击杀增加多少；MISS、无效输入和单纯烟雾不计分。每次蚊香记录本次击杀、本次保留虫卵数，以及本局最大单次蚊香击杀。

清场次数按成功发动蚊香的次数统计，不要求清场后再也没有蚊子。无成蚊时蚊香不可发动，不刷使用次数。

## 6. 蚊香清场与虫卵回弹

### 6.1 唯一逻辑定义

蚊香是一种瞬时清除成蚊的能力，没有持续伤害、残留毒区或对新生蚊子的免疫封锁。

在命令执行时，依次完成：

1. 检查本局状态、解锁、成蚊数量、冷却，以及是否已处于回弹观察窗。
2. 设置蚊香冷却至 `当前 Tick + 200`。
3. 记录当前成蚊与虫卵数量。
4. 公蚊、母蚊归零，清空母蚊待产卵批次，累加击杀与清场统计。
5. 虫卵桶保持原始孵化时点，不移动、不复制、不延后。
6. 若仍有虫卵，进入 10 Tick，即 0.5 秒的 `HatchAfterClear` 回弹观察窗。
7. 本 Tick 继续执行正常产卵和孵化阶段；无虫卵时由统一结束判定进入结算。

### 6.2 回弹观察窗

该状态用于 UI 与视听强调，所有虫卵仍由同一孵化系统处理。新生母蚊照常登记首次产卵时间，不为动画改变出生时间。

观察窗内允许手掌和电蚊拍，禁止再次使用蚊香。观察窗持续到清场 Tick 后第 10 Tick 的事件结算完成；若更早满足繁殖结束条件，直接结束本局。

清场时已有虫卵必然在其原定时点孵化，不晚于该观察窗结束。它们的孵化总量应与清场时虫卵数一致，但观察窗结束时的成蚊数可能因后续攻击而少于该数量。

### 6.3 例子与边界

| 清场时状态 | 正确结果 |
|---|---|
| 1,000 成蚊、30 枚虫卵，分别剩余 0.1 / 0.3 / 0.5 秒 | 成蚊先清零；各批虫卵按原时点孵化，不强制改为同一时刻 |
| 1,000 成蚊、0 枚虫卵 | 成蚊清零后进入繁殖结束，不生成隐藏后代 |
| 蛋在当前 Tick 到期，同时收到蚊香命令 | 先清掉旧成蚊，后孵化这批蛋；同 Tick 结束时成蚊可以已非零 |
| 0 成蚊、有虫卵 | 蚊香按钮不可用，虫卵正常等待孵化 |
| 清场后最后的母蚊又被手掌击杀且无蛋 | 进入繁殖结束，不强制等待观察窗到期 |

不要求所有清场都出现 0.1–0.2 秒完全空屏。烟雾和音频可以表现冲击，但不能隐藏已经出生的可攻击成蚊到与逻辑明显脱节。UI 分别显示“本次清除 X”与“幸存虫卵 Y”，不把二者合并为一个模糊数字。

## 7. 状态机、时间与事件顺序

### 7.1 状态分层

| 本局状态 | 推进模拟 | 接受攻击 | 说明 |
|---|---|---|---|
| Running | 是 | 是 | 正常繁殖与操作 |
| HatchAfterClear | 是 | 手掌、电蚊拍 | 0.5 秒回弹强调阶段；不另设孵化队列 |
| ReproductionEnded | 否 | 否 | 显示结算，允许重开或返回主菜单 |

暂停是覆盖在本局状态上的独立标记，包含菜单、失焦和性能保护等原因。暂停时保留原本局状态与回弹剩余 Tick，所有游戏计时停止。主菜单不驱动模拟。

### 7.2 固定模拟步长

- 一个 Tick 为 50ms，即每模拟秒 20 Tick；时间规则均转换为整数 Tick。
- 新局从 Tick 0 开始，初始母蚊首次产卵安排在 Tick 20。
- 母蚊周期和首次产卵延迟为 20 Tick，虫卵孵化延迟为 10 Tick。
- 逻辑使用单独的固定步长驱动器，不依赖每帧 Update 的次数，也不把物理帧率当作繁殖规则。
- 一条输入命令只在被分配的下一个有效模拟 Tick 执行；帧率对结果的一致性以相同 Tick 的命令日志为比较条件。

### 7.3 同一 Tick 的唯一顺序

1. 模拟 Tick 增加 1；用当前 Tick 与 `readyAtTick` 比较武器是否冷却完成。
2. 处理本 Tick 的攻击命令。被击杀母蚊从其待产卵批次立即移除。
3. 结算本 Tick 到期的母蚊产卵，生成未来 10 Tick 孵化的虫卵，并把存活母蚊重新安排至未来 20 Tick 产卵。
4. 合并并孵化本 Tick 到期的虫卵；新母蚊安排至未来 20 Tick 首次产卵。
5. 在每个数量事务完成后更新当前总数、峰值、纪录与解锁，而不是只取渲染帧结束时的数值。
6. 判断繁殖结束；否则检查回弹观察窗是否结束。
7. 验证数量不变量，发布最终 HUD 快照及有序表现事件，处理必要存档请求。

同 Tick 在步骤 4 才解锁的武器，最早在下一 Tick 使用。步骤 2 的攻击不能攻击步骤 4 才出生的蚊子；蚊香也不会消灭这些后代。

### 7.4 卡顿、暂停与恢复

短暂掉帧时按固定 Tick 补进度，保留每个 Tick 的事件顺序，不能用当前母蚊数乘以整段耗时替代多代繁殖。

建议单个渲染帧最多处理 5 个逻辑 Tick，多余积欠留待后续帧处理；若积欠超过 1 秒，进入性能保护暂停，保留尚未处理的积欠并显示“模拟已暂停”。用户继续时先在加载界面处理已积欠的有限 Tick，再恢复新输入；暂停期间不再积累时间。该阈值属于可调工程预算。

完整的未处理积欠以 `pendingCatchupTicks` 记录并持久化。继续或读档补算时，先解除逻辑步进的暂停标记，但保持新输入及墙钟累积关闭；驱动器在每次调用 Step 前先把积欠减 1，确保 Tick 末快照里的积欠已扣除当前一步。处理完再开启正常输入和计时。若补算中进入繁殖结束，清除剩余积欠，因为本局已无后续模拟；加载界面处理也必须分帧，不能长时间阻塞主线程。

失焦、最小化、关闭程序或正常菜单暂停的持续时间不计入积欠，不产生离线收益。系统休眠、唤醒或检测到单帧墙钟跳变超过 1 秒时，优先进入恢复暂停并忽略该整段墙钟间隔；恢复过程中不补算系统休眠时间。正常暂停前不足一 Tick 的计时余数保留在内存，持久存档只保存完整 Tick 边界，最多产生不足 50ms 的墙钟差。

## 8. 数值基线与首次体验

### 8.1 基线测算

前次分析使用 5,000 个种子的脚本模拟，在不攻击、初始母蚊 1 秒首产、新母蚊出生满 1 秒首产、0.5 秒孵化和逐只 50% 性别判定下得到：

| 成蚊阈值 | 从开局累计用时中位数 | 10%–90% 分位区间 |
|---|---:|---:|
| 10 | 3.5 秒 | 3.0–3.5 秒 |
| 100 | 10.0 秒 | 9.0–11.5 秒 |
| 1,000 | 16.5 秒 | 15.5–18.5 秒 |

这些是规则脚本的测算结果，不是 Unity 实机性能或真人试玩结论。测算至首次达到 1,000 只，与本版的小批量性别规则一致；不能直接外推本版大数阶段的随机波动。

v1.0 中“10→100 用 20–60 秒、100→1,000 用 30–90 秒”的期望不作为本版通过条件。本版先保留原机制的快速增长特征，再通过完整原型决定是否修改参数。

### 8.2 武器效能与风险

持续按冷却使用且始终有足够目标时，手掌期望消灭约 1.67 只/秒，电蚊拍最多消灭 12.5 只/秒；混合使用受输入 Tick 限制，不能简单承诺两者之和。

这两种武器在大数阶段影响会快速降低。本版接受其主要承担早期反馈的职责，不通过偷偷按比例扣除数量保持强度。蚊香是后期主要操作；若后期只能等待且没有继续意愿，应调整核心循环，而不是把这一问题当作性能优化解决。

### 8.3 首次五分钟体验目标

下表为体验目标，实际触发由数量与行为驱动，不能为了按时展示而暗改繁殖速度。

| 时段参考 | 内容 | 希望玩家理解或执行 |
|---|---|---|
| 0–5 秒 | 四只开局、首批产卵与孵化、手掌解锁 | 母蚊产生虫卵，虫卵会变成蚊子 |
| 5–20 秒 | 手掌 MISS、电蚊拍与蚊香陆续解锁 | 武器变强，但增长更快；非阻断提示可稍后阅读 |
| 20–60 秒 | 首次清场与可能的虫卵回弹 | 清成蚊不等于清掉虫卵；也可能真正停止繁殖 |
| 1–3 分钟 | 重复清场、刷新本次与最佳击杀数字 | 玩家愿意主动再次使用武器 |
| 3–5 分钟 | 更大数量层级、回看本局纪录 | 判断单场景循环是否仍有趣，或选择重开 |

首版里程碑为三把武器解锁，以及本局累计击杀达到 1K、1M、1B。里程碑只提供提示和纪录，没有暗中增加攻击力或繁殖倍率。

### 8.4 平衡验证方法

至少比较三种行为：完全不操作；始终使用当前最强已解锁武器且冷却就绪就攻击；预先固定的间歇操作日志。使用相同种子组，记录各阈值时间、繁殖结束比例、首次蚊香时虫卵数、累计击杀及第二次清场时间。

规则调参采用新配置版本，在新局生效；原存档继续沿用其配置快照。原型阶段先验证体验，再决定是否改解锁阈值、冷却或基础繁殖规则，不能同时改多个参数却无法解释效果。

## 9. UI、提示与数字表达

### 9.1 界面清单

| 界面 | 内容 |
|---|---|
| 主菜单 | 开始新局、继续游戏、历史纪录、设置、退出 |
| 游戏 HUD | 当前成蚊、公母数、虫卵、实际产卵速率、三把武器、当前选择、冷却与解锁条件 |
| 清场反馈 | 本次清除数量、幸存虫卵数、是否刷新单次纪录 |
| 暂停菜单 | 继续、重新开始、设置、保存并返回主菜单、保存并退出 |
| 繁殖结束 | 结束原因、本局累计击杀、最大清场、数量峰值、模拟时长、重开与返回 |
| 设置 | 分辨率、窗口模式、音量、UI 缩放、闪光与震屏 |

主动重开时提示“将替换当前局进度，历史纪录保留”，由玩家确认；这是一项游戏内流程，不是开发文档审批。

### 9.2 1920×1080 参考布局

```text
┌────────────────────────────────────────────────────────────┐
│ 成蚊 12.8K       公 6.4K / 母 6.4K              [暂停]      │
│ 虫卵 2.1K       最近 1 秒产卵 4.8K                         │
│                                                            │
│                 固定镜头 3D 场景与蚊群                     │
│                 点击场景使用已选武器                       │
│                                                            │
│ 本次清除 10.2K       幸存虫卵 1.8K                         │
│                                                            │
│ [1 手掌 0.3s]      [2 电蚊拍 就绪]      [3 蚊香 7.4s]       │
│ 本局累计击杀 25.3K       本局最大清场 10.2K                 │
└────────────────────────────────────────────────────────────┘
```

当前成蚊与虫卵是最高优先级信息；产卵速率显示过去 20 Tick 的真实产卵总量，不直接把包含未首产母蚊的数量标成实际每秒产卵数。

### 9.3 提示与刷新

- 关键事件、清场结果和解锁立即更新；普通大数字以不低于 10Hz 刷新，避免每帧重复格式化大数。
- 第一次手掌 MISS 提示“手掌有 50% 几率落空”；第一次蚊香提示“只清除成蚊，虫卵仍会孵化”。
- 提示不暂停模拟，不自动切换武器。同类提示合并，不在屏幕堆积几十条。
- 解锁进度使用本局峰值，如“本局峰值 87 / 100”，防止与当前数量混淆。
- 数字归零和武器是否可用必须跟随真实逻辑，不为数字滚动动画延迟状态。

### 9.4 数字格式

0–999 显示整数；其后依次使用 K、M、B、T。达到 10^15 后使用科学计数法，例如 `1.28e15`。统一保留约三位有效数字，格式化舍入跨界时进位，例如接近一百万应显示 1.00M，而不是 1000K。

逻辑和存档保留准确整数。格式化只读取数量级和前几位，不先把整个大整数转为浮点数。可以在统计详情中查看完整整数，但对超长数字提供复制或折叠，避免撑破 HUD。

支持 100%、125%、150% UI 缩放；1280×720 下仍能读到核心数字与武器状态。宽屏保持场景构图和 HUD 安全区，不让关键按钮随比例被裁切。

## 10. Unity 技术设计

### 10.1 工程基线

采用 Unity、C# 和 URP。项目创建时选定受支持的稳定编辑器版本，记录具体补丁号、URP 与 Input System 包版本、脚本后端和构建目标，提交到版本控制。本文不指定未经实际安装验证的“最新版”。

HUD 建议采用 uGUI + TextMeshPro；输入统一经 Input System 转为逻辑命令。工程配置与平衡数据使用 ScriptableObject，运行时转换为本局不可变配置快照。纯逻辑程序集不依赖场景对象、渲染、音频或 Unity 物理查询。

### 10.2 模块与职责

| 模块 | 职责 | 不应承担 |
|---|---|---|
| SimulationCore | 时间、数量、母蚊批次、虫卵批次、终止条件 | 驱动模型动画 |
| WeaponService | 输入合法性、冷却、随机击杀、清场 | 根据粒子数量决定击杀 |
| ProgressService | 本局解锁、纪录、里程碑 | 自动改变繁殖参数 |
| SimulationDriver | 固定 Tick、输入时间分配、暂停与积欠管理 | 每帧按 dt 直接增加虫卵 |
| PresentationController | 代表蚊子、武器动作、音效、事件顺序 | 创建真实数量的蚊子对象 |
| HUDPresenter | 格式化快照、按钮状态、提示 | 保存独立于逻辑的数量副本作为真值 |
| SaveService | 保存、校验、加载、版本兼容 | 关闭后计算离线繁殖 |
| DebugPanel | 固定种子、单步、数据注入、性能查看 | 将测试成绩混入正式本地纪录 |

事件载荷包含 Tick、事件序号、数量变化和必要的性别击杀结果。使用聚合事件，例如一次 `AdultsKilled(count, femaleKilled)`，而不是向表现层发送一百万条死亡通知。

### 10.3 权威数据结构

| 字段 | 建议类型 | 说明 |
|---|---|---|
| simulationTick | long | 完整模拟 Tick 数 |
| pendingCatchupTicks | int | 驱动器已累计、尚未处理的完整 Tick；与权威状态一起保存 |
| maleCount | BigInteger | 当前公蚊数 |
| femaleLayBuckets | 到期 Tick → BigInteger | 所有活母蚊按下一次产卵时点分组，包含尚未首产者 |
| femaleCount | BigInteger 派生缓存 | 必须等于所有母蚊桶之和 |
| eggBuckets | 到期 Tick → BigInteger | 所有尚未孵化的虫卵 |
| eggCount | BigInteger 派生缓存 | 必须等于所有虫卵桶之和 |
| adultCount | 派生值 | maleCount + femaleCount |
| weaponReadyAtTicks | 武器 ID → long | 冷却完成时点 |
| unlockFlags | 位标记 | 当前局已解锁武器 |
| runPhase / burstEndTick | 枚举 / long | 本局状态与回弹截止时点 |
| burstInitialEggCount / burstHatchedCount | BigInteger | 本次清场幸存蛋数与已孵化数，用于反馈校验 |
| runStats | 结构体 | 本局击杀、产卵、孵化、峰值与尝试次数 |
| recentLayHistory | 最近 20 Tick 的计数 | HUD 实际产卵速率，读档后不凭空清零 |
| simulationRngState | 可序列化结构 | 逻辑随机状态、算法版本、初始种子 |
| runConfig | 配置快照及标识 | 本局规则版本、随机分配模式、参数 |
| runId / nextEventSequence | 标识 / long | 识别局次、防止重复统计、恢复事件序号 |

字典或有绝对到期 Tick 的时间桶均可。基础配置下，处理完一个 Tick 后，母蚊未来到期键最多 20 个，虫卵未来到期键最多 10 个；数量巨大时只是桶内整数变大。

若使用环形数组，必须存储绝对到期 Tick，保证“当前待取出的桶”不会被“同索引的新未来事件”覆盖。不能只按 `tick % delay` 取索引后直接覆盖数据。

### 10.4 数值与随机抽取

本版采用 `System.Numerics.BigInteger` 表示逻辑数量，保持加、减、比较、总量守恒和扣除 1 / 10 只的准确性。UI 缩写与大批量性别统计近似均不改变已结算整数的准确存储。

不使用 C# `decimal` 代替无限整数，也不以 `double` 保存成蚊真值。每次随机击杀只抽取一个 `[0, adultCount)` 整数：前 `maleCount` 个位置属于公蚊，其后依次属于按到期 Tick 升序排列的母蚊桶。同一个整数同时确定性别和母蚊所在桶，不再二次抽取母蚊桶。这个区间顺序在运行时、存档恢复后和测试中完全相同，禁止依赖字典的隐式枚举顺序。

使用随机位与拒绝采样避免对一个有限随机整数直接取模造成偏差。采样成本取决于数值位数，而不是成蚊只数。

正式实现前选择并固定可导出完整状态的确定性随机数算法，提供已知输入输出测试向量。不要依赖无法完整恢复内部状态的随机 API 来承诺存读档结果完全一致。

### 10.5 数量不变量

每个完整 Tick 后验证：

- 所有数量非负；`adultCount = maleCount + femaleCount`。
- `femaleCount = sum(femaleLayBuckets)`，`eggCount = sum(eggBuckets)`。
- `adultCount = 4 + totalHatched - totalKilled`。
- `eggCount = totalEggsCreated - totalHatched`。
- `femaleCount = 2 + totalFemaleBorn - totalFemaleKilled`，公蚊同理。
- 母蚊及虫卵桶中不应残留已经完成处理的到期事件。
- `runMaxAdult >= adultCount`；解锁集合只能增加，重开除外。

所有出生、死亡与统计通过原子数量事务提交，避免界面短暂看到公母相加与总数不符。调试注入必须同时更新不变量对应的基线或统计，并把该局标为测试局。

### 10.6 建议目录

```text
Assets/Game/
  Art/Models/           # 导出的 FBX
  Art/Textures/
  Art/Materials/
  Audio/
  Configs/
  Prefabs/
  Scenes/               # MainMenu、Game
  Scripts/Core/
  Scripts/Presentation/
  Scripts/UI/
  Scripts/Persistence/
  Tests/EditMode/
  Tests/PlayMode/
SourceArt/Blender/       # .blend 源文件，位于 Assets 之外
Builds/                 # 本地构建输出，不提交大型中间产物
Docs/                   # GDD、资产清单、测试与性能报告
```

源代码、项目设置、包锁定文件及 Unity `.meta` 文件进入版本控制。Library、Temp、Logs 和构建中间产物不进入版本控制；大型二进制源资产可在需要时使用 Git LFS。

## 11. Blender 资产与制作规范

### 11.1 风格方向

首版采用风格化、轮廓清楚的 3D 美术。蚊子略微夸张，突出长嘴、翅膀和腹部，避免写实细节在远景中变成噪点。场景为一个卧室角落，主要包含墙面、地面、桌面或床边等少量道具。

公母优先共用基础网格，使用腹部形状或配色差异区分；不要让色差成为唯一信息来源，HUD 仍明确显示公母数量。

### 11.2 MVP 资产清单

以下数量和复杂度是首轮预算，需在目标镜头下校准，不是要求美术刻意做满预算。

| 资产 | 数量基线 | 交付内容 | 首轮预算 / 验收要点 |
|---|---:|---|---|
| 蚊子基础模型 | 1 套，公母 2 种外观 | FBX、贴图、材质说明、源文件 | 常用网格约 300–800 三角面，共用材质；极密集群体可用简化表示 |
| 蚊子运动 | 飞行、受击、死亡 | 动画或程序驱动参数 | 翅膀优先用轻量变换或 Shader；不要求每只配完整 Animator |
| 手掌 | 1 | 前景模型、待机/拍击/落空动作 | 约 2K–5K 三角面，只制作可见部分 |
| 电蚊拍 | 1 | 模型、攻击动作、电弧挂点 | 约 1K–3K 三角面，明确握持原点 |
| 蚊香 | 1 | 模型、烟雾挂点 | 约 500–1.5K 三角面，无需持续燃烧物理模拟 |
| 房间角落 | 1 场景 | 墙、地、主要家具等模块 | 初始静态场景总预算约 50K 三角面，镜头外无需精做 |
| 虫卵表示 | 1 套 | 小点粒子或简化图标 | 显示在固定虫卵区域，数量与显示分离 |
| 武器与状态图标 | 3 个武器 + 基础 UI 图标 | 带透明背景的图像或项目内矢量资源 | 统一视觉尺寸，冷却状态可辨识 |
| 特效 | 拍击、电弧、烟雾、孵化、死亡 | Unity Prefab、贴图、参数 | 所有特效有生命周期与数量预算 |
| 音频 | 嗡鸣、拍击、扑空、电流、清场、解锁 | 可用音频文件、循环与响度说明 | 授权来源可追溯；嗡鸣无接缝循环 |

常用贴图建议 512–1024，重要前景资产可按实际占屏提高。应优先减少材质种类和透明层叠，不只盯住三角面数量。

### 11.3 导出与导入约定

1. `.blend` 为源文件，放在 Unity `Assets` 之外；运行项目使用导出的 FBX、贴图与 Unity 材质。
2. 统一米制，场景以 Unity 1 单位对应 1 米为基准；蚊子可为可读性夸张，但需在资产表记录设计尺寸。
3. 建立一套验证过的 Blender→FBX→Unity 轴向与单位预设。不要在不同资产上混用“导出补转 90 度”和“Prefab 再补转 90 度”。
4. 在绑定与动画制作前确认缩放、旋转和原点；绑定后修改 Transform 必须重新检查动画与导出结果，不能机械地对所有骨架 Apply。
5. 蚊子原点置于身体附近；武器原点与挂点按握持和动画需要设置。命名如 `M_Mosquito`、`M_Zapper`、`FX_SmokeAnchor`。
6. 贴图单独交付，材质在 Unity URP 中配置并检查。Blender 渲染效果不作为已证明 Unity 材质一致的依据。
7. 需要约束驱动的动作按选定导出流程烘焙；动画片段采用统一命名，如 `Hand_Idle`、`Hand_Attack`、`Hand_Miss`。
8. 首个资产先验证大小、朝向、法线、透明翅膀、动画和材质，确认模板后再批量生产。

### 11.4 资产验收

资产在最终镜头及 720p、1080p 下均需检查：轮廓是否可辨、背景对比是否足够、翅膀是否闪烁、动作是否穿过 HUD、武器原点是否正确、纹理是否缺失。公母区别应能在近景样例中辨认，但玩法不要求玩家逐只辨别性别再攻击。

检查 FBX 导入后没有异常缩放、多余摄像机或灯光、无用材质和损坏动画。资产验收图以 Unity 运行画面为准，Blender 展示图作为辅助参考。

## 12. 视觉、动画与音频

### 12.1 代表蚊子与真实数量

默认存活代表蚊子最多 300 只。数量不超过此上限时，在事件表现完成后的稳定画面中，一只模型对应一只当前成蚊；逻辑仍使用统计与时间桶。超过上限时，模型只表示群体，界面保留简短说明“画面展示部分蚊群”。

不存在基于每只模型的真实碰撞击杀，所以代表模型不需要拥有可影响逻辑的独立生命值。逻辑击杀结果先确定，再选取性别匹配的代表模型播放死亡；高数量阶段允许少量模型概括群体事件。

在高数量阶段杀死 10 只，逻辑只能减 10；可播放最多 10 个死亡反馈并显示 `-10`，不让粒子爆炸误导为额外击杀。死亡反馈后代表数量可以回到显示上限，但新表示应淡入，避免同一模型闪灭后立即原地复活。

### 12.2 数量层级

| 成蚊范围 | 表现目标 | 实现方向 |
|---|---|---|
| 0 | 空场且嗡鸣归零 | 不残留活蚊代表或错误循环声音 |
| 1–30 | 看清每只蚊子与攻击反馈 | 稀疏飞行、明显单只动作 |
| 31–300 | 逐渐拥挤 | 增加活动密度和群体移动 |
| 301–10K | 明确超过可见数量 | 代表上限 + 稀疏群体粒子与数字提示 |
| 10K–1M | 强化规模变化 | 调整群体层次、运动和环境色调，不线性增加对象 |
| 1M 以上 | 保留可读性与夸张感 | 有限密度档位、数量级提示、受限音频变化 |

档位切换使用数量级和插值，不把完整 BigInteger 强制转为 float。声音和闪光有最大强度，不随数量无限增大。

### 12.3 事件反馈表

| 事件 | 视觉 | 音频 |
|---|---|---|
| 产卵 | 虫卵区域出现少量代表点或数量跳动 | 默认静默，避免每枚蛋播放声音 |
| 孵化 | 代表点闪烁、有限蚊子淡入 | 群体嗡鸣随真实成蚊状态变化 |
| 手掌成功 | 手掌动作、目标死亡、`-1` | 清脆拍击 |
| 手掌 MISS | 落空动作、MISS | 短促扑空 |
| 电蚊拍 | 前景电弧、有限死亡代表、实际击杀数字 | 单次电流效果 |
| 蚊香 | 清除旧成蚊表示、短促烟雾、清场数字 | 清场冲击；嗡鸣按真实数量衰减 |
| 虫卵回弹 | 已到期虫卵对应的新蚊群出现 | 嗡鸣恢复；不强制等待静默期 |
| 繁殖结束 | 空场或剩公蚊，结算卡片 | 嗡鸣淡出、轻量结束提示 |

同 Tick 清场与孵化时，先清除旧表示，再创建后代表示。不得因延迟播放一个旧的清场动画而把刚出生的后代模型再次删除。

### 12.4 音频与舒适性

嗡鸣采用少量群体循环音源，禁止每只蚊子一个 AudioSource。音量上限固定，数量变化主要通过音色和层次表达。支持总音量、音效音量、嗡鸣音量独立调节。

提供关闭震屏与降低闪光选项。烟雾不持续遮挡 HUD，电弧不覆盖整个画面高频闪烁。默认保持轻量效果，设置调整即时预览且不改变玩法规则。

## 13. 存档、暂停与本地纪录

### 13.1 保存范围

首版支持一个当前局存档，以及独立含义的本地历史纪录。两者可以放在同一原子快照中，避免当前局与历史纪录写入不同步。

| 数据 | 继续游戏 | 重新开始 |
|---|---|---|
| 当前公蚊、母蚊桶、虫卵桶 | 恢复 | 重置为 2 公、2 母、0 蛋 |
| 当前 Tick、冷却、回弹状态 | 恢复 | 重置 |
| 本局解锁与统计 | 恢复 | 重置 |
| 本局配置与逻辑随机状态 | 恢复 | 使用当前默认配置与新种子 |
| 历史单局累计击杀最高纪录 | 保留 | 保留 |
| 历史最大单次蚊香击杀、成蚊峰值 | 保留 | 保留 |
| 首次提示已读状态、显示与音量设置 | 保留 | 保留 |

“历史单局累计击杀最高纪录”是各局结果的最大值，不是所有局击杀求和。更新采用 `max`，不是每次保存把当前累计击杀再次加到历史上。首版不统计全局总击杀，避免与本局累计和读档重复计数混淆。

### 13.2 存档内容与时机

存档 DTO 至少包含：schemaVersion、simulationVersion、runId、配置快照及 hash、逻辑 Tick、pendingCatchupTicks、公蚊数、母蚊/虫卵桶、所有武器 readyAtTick、本局状态、回弹状态与计数、本局峰值及统计、最近 20 Tick 产卵历史、解锁与里程碑、逻辑 RNG 算法版本及状态、事件序号、是否测试局，以及本地历史纪录。

BigInteger 转为十进制字符串序列化，不把它交给只支持固定精度数值的 JSON 消费端。桶顺序按到期 Tick 排序，确保保存和加载不会改变随机加权遍历顺序。已经结算的本局也可保存，并恢复到结算界面。

建议保存时机：每 10 个模拟秒自动保存；解锁与合法蚊香操作所在 Tick 完成后；繁殖结束；菜单保存返回；正常关闭窗口。每个 Tick 最多合并一次存档请求。新局初始化完成后也保存，以使“开始新局”明确替换旧当前局。

所有快照都在完整 Tick 边界创建，文件 I/O 可异步执行，但使用不可变快照，并按递增保存序号提交，禁止旧异步写入覆盖新快照。临时文件写完并校验后再替换正式文件，保留上一份有效备份。具体原子替换方式应在目标 Windows 文件系统验证。

暂停、结算、菜单返回和正常退出时，主循环可直接从最近完整 Tick 的静止状态与驱动器积欠创建快照，不等待下一次 Step；暂停不能阻止保存请求执行。正常退出需等待当前最新快照提交成功，失败时提供重试或明确放弃本次保存的提示。恢复存档时若有完整 Tick 积欠，按第 7.4 节先补算，再接受玩家新输入。

读档校验非负数量、桶总数、到期时点、解锁合法性、配置版本和校验信息。失败时尝试有效备份；仍失败则提示无法继续并保留原文件，不静默清空历史纪录。首版可不迁移未知旧 schema，但必须明确提示，不能当成正常新局读入。

### 13.3 暂停政策

| 场景 | 模拟 / 冷却 / 计分时钟 | 恢复方式 |
|---|---|---|
| Esc 或设置菜单 | 全部停止 | 点击继续 |
| Alt-Tab、失焦、最小化 | 全部停止 | 回来显示暂停覆盖层，点击继续 |
| 系统休眠与唤醒 | 全部停止，不补休眠时间 | 恢复暂停覆盖层 |
| 退出并重新打开 | 不产生离线增长 | 主菜单选择继续 |
| 繁殖结束 | 全部停止 | 只能重开或返回菜单 |
| 性能保护暂停 | 全部停止，保留既有有限积欠 | 按第 7.4 节处理后继续 |

暂停时清除尚未执行的攻击命令和按住状态，防止恢复瞬间误触蚊香。已在完整 Tick 内执行的攻击不可撤销。继续游戏和读档后均要求新的主动输入。

正常暂停菜单下还允许调整设置和保存，但不更新模拟纪录。存档时间戳只用于文件说明，不参与繁殖计算。

## 14. 性能目标与降级

### 14.1 基准条件

以下为原型验收预算，尚未实测，不构成最低硬件宣传。建议参考测试机为 Windows 11 x64、Intel Core i5-10400 或接近性能的 CPU、GTX 1650 4GB 或接近性能的 GPU、16GB 内存、SSD。实施时记录实际机器、驱动、Unity 版本与构建参数；若团队测试机不同，应明确更新基线后再比较。

在 1920×1080、默认画质、独立 Windows 构建中测试。编辑器运行数据用于定位问题，不代替发布构建验收。

| 指标 | 首轮目标 |
|---|---|
| 常规帧率 | 目标 60 FPS；稳定测试区间帧耗时 P95 ≤ 16.7ms，P99 ≤ 33.3ms |
| 逻辑 Tick 耗时 | P95 ≤ 1ms，范围包括繁殖、击杀、大数和统计，排除异步文件 I/O |
| 视觉对象 | 活蚊代表 ≤ 300；死亡反馈代表 ≤ 30；统计时分别计数 |
| 粒子 | 场景同时活跃粒子初始预算 ≤ 1,500，必要时进一步降低 |
| 内存 | 游戏进程工作集初始目标 ≤ 1GB；预热后 30 分钟无持续无界增长 |
| 清场与回弹 | 不创建与逻辑击杀数量成比例的对象、事件、日志或音源 |
| 数量测试范围 | 至少覆盖 10^6、10^12、10^30、10^100、10^300 的注入状态 |
| 稳定性 | 连续运行 30 分钟，覆盖手掌连发、电拍、清场、暂停与存读档 |

统计性能时排除启动和显式加载界面，并分别报告清场、自动保存、普通运行的帧耗时。若未达预算，先定位瓶颈再调整资产或指标，不只用平均 FPS 掩盖尖峰。

### 14.2 “无限”的工程含义

游戏不设置因蚊子数量过大而强制结束的玩法上限，但有限硬件不可能提供数学意义的无限容量。BigInteger 的存储和运算仍随数值位数增长；10^300 是本版最低大数测试覆盖，不是到此截断的玩法上限。

超出测试覆盖时保持相同整数规则。若逻辑、格式化或存档遇到资源压力，执行可见暂停并保留最近有效存档，不静默夹断数值、丢弃虫卵或把数量转成浮点无穷大。

### 14.3 降级顺序

先减少烟雾、背景群体粒子、阴影和后处理，再把活蚊代表上限从 300 调至 200 或 100。HUD 真实数量、武器规则、孵化时点和击杀效果不变。

实现上先验证对象池、统一运动更新、共用材质及简单动画。仅当实测瓶颈支持时再引入 GPU 实例化、Jobs 或其他复杂方案；不把逐只逻辑对象迁移到 ECS 当作解决无限数量的前提。

池扩容仅发生在明确初始化或质量变更阶段，攻击过程中优先复用。音频并发、飘字和临时事件队列也需有上限或合并策略，日志禁止每只蚊子输出一条。

## 15. MVP 范围与实施里程碑

### 15.1 MVP 必须交付

- 一个可运行的 Windows x64 构建，固定镜头单场景，完整键鼠输入。
- 四只开局、母蚊批次产卵、虫卵定时孵化、确定性的混合性别分配。
- 三把武器、独立冷却、本局峰值解锁、全局随机击杀。
- 正确清场、保留原虫卵时点、回弹反馈与繁殖结束结算。
- 当前数量、累计击杀、单次清场、里程碑及历史纪录显示。
- 暂停、失焦处理、设置、单槽继续游戏、备份与错误提示。
- 有限视觉表示、大整数数量、性能统计与最低测试覆盖。
- 最少一轮外部试玩记录，用于判断全武器解锁后的体验。

### 15.2 实施顺序

未提供团队规模和工期，本版以完成条件安排里程碑，不虚构日历承诺。

| 里程碑 | 产出 | 完成条件 |
|---|---|---|
| M0 工程与资产验证 | Unity 空工程、构建、蚊子 FBX 样例、版本清单 | 能独立运行；模型大小、方向、材质和动作正确 |
| M1 纯逻辑原型 | 时间桶、随机数、武器、纪录与无界面模拟 | 关键规则测试通过；无负数、重复孵化、死后产卵 |
| M2 灰盒可玩原型 | 场景、HUD、输入、清场回弹与结算 | 玩家可以完整启动、操作、结束及重开 |
| M3 美术与声音 | 正式蚊子、武器、场景、特效音频 | 三种武器反馈可辨，视觉与逻辑一致 |
| M4 PC 完整性 | 存档、暂停、设置、性能预算 | 存读档、失焦及基准构建测试通过 |
| M5 试玩与调参 | 试玩记录、参数版本、修订清单 | 判断是否支持至少五分钟的自愿重复操作 |

程序与美术可以并行推进，但先完成 M0 的资产导入样例，再批量生产模型。M2 已能进行第一次体验验证，避免等全部美术完成后才发现循环缺少乐趣。

## 16. 验收测试与试玩评估

### 16.1 测试约定

逻辑测试使用固定种子、固定 Tick 输入和可注入状态。需要测试未自然解锁的武器时，显式设置本局峰值与解锁标记；注入桶、数量和累计统计必须一致。测试局不写正式历史纪录。

“立即清零”指攻击事务执行完的瞬间；若该 Tick 随后有虫卵孵化，最终成蚊不为零是正确行为。测试必须区分事件快照与 Tick 末快照。

### 16.2 逻辑与武器

| ID | 场景 | 通过条件 |
|---|---|---|
| L01 | 新局推进至 Tick 20 | 初始母蚊生成 2 蛋；成蚊仍为 4，蛋到期为 Tick 30 |
| L02 | 继续至 Tick 30 | 两蛋孵化，成蚊为 6；新母蚊首次产卵不早于 Tick 50 |
| L03 | 在一只母蚊产卵 Tick 用必中测试攻击杀死它 | 该母蚊本 Tick 及未来均不产卵；已经生成的蛋保留 |
| L04 | 成蚊首次达到 10 / 100 / 1,000 | 对应解锁；低于阈值后不重新上锁，新局重新锁定 |
| L05 | 手掌强制命中与强制 MISS 各一次 | 命中只减 1；MISS 不减数量；两者消耗 6 Tick 冷却 |
| L06 | 手掌在 Tick t 使用后再次输入 | t+5 不可用，t+6 可用；电拍与蚊香同理验证 16 / 200 |
| L07 | 当前 6 成蚊，无当前 Tick 孵化，使用电拍 | 恰好清除 6，不出现负数，母蚊桶同步扣空 |
| L08 | 0 成蚊但有未来虫卵时使用各武器 | 无效，不计尝试、击杀或冷却，不消耗逻辑 RNG |
| L09 | 注入 100 成蚊与 30 枚未来到期蛋，解锁蚊香 | 清场事务后成蚊 0、蛋 30、母蚊桶空、冷却为当前+200 |
| L10 | L09 后不攻击，推进至所有原蛋到期 | 共孵化 30，原 deadline 不变，无丢失与重复 |
| L11 | 当前 Tick 的到期蛋与蚊香同时发生 | 先清旧成蚊再孵化，最终后代存活，不被旧清场动画误删 |
| L12 | 回弹窗内攻击 | 手掌、电拍可合法消灭后代；蚊香被拒绝，无重复统计 |
| L13 | 有成蚊、无蛋时使用蚊香 | Tick 末进入繁殖结束，不补新蚊子 |
| L14 | 母蚊 0、蛋 0、公蚊大于 0 | 繁殖结束；剩公蚊不再可击杀刷分 |
| L15 | 母蚊 0、有蛋，最后一批性别指定为全公 | 孵化前保持运行，全部孵化后结算 |
| L16 | 小批量 0 / 1 / 2 / 1,024，大批量 1,025 及奇偶大数 | 各次公母之和等于孵化量；阈值两侧符合第 4.3 节 |
| L17 | 注入 10^300 级数量后手掌命中 / 电拍 / 蚊香 | 分别准确减 1 / 10 / 全部；无浮点截断，虫卵不受影响 |
| L18 | 30 / 60 / 144 渲染 FPS，重放相同 Tick 命令日志 | 相同种子下，每个检查点的权威状态完全一致 |
| L19 | 全程随机操作及多次清场 | 每个 Tick 满足第 10.5 节全部不变量 |

概率验证与逻辑门禁分开：使用固定且记录在案的多种子样本，验证手掌长期命中率约为 50%；例如 10,000 次合法尝试采用 47%–53% 的宽容差作为偏差预警。精确边界通过注入随机结果验证，不因偶然的一组抽样结果让普通构建随机失败。大批量性别分配按其明确算法验证，不套用小批量二项方差要求。

### 16.3 PC、存档与表现

| ID | 场景 | 通过条件 |
|---|---|---|
| P01 | 点击 UI、切换武器、冷却中连续输入 | 不穿透，不延迟排队，不重置冷却 |
| P02 | 按住攻击后失焦，再返回 | 自动暂停，恢复后须重新按下，不误触蚊香 |
| P03 | Esc / 设置暂停 30 秒 | 数量、蛋到期、冷却、模拟时长全部不变 |
| P04 | 关闭程序 30 秒后继续 | 无离线增长；恢复原 Tick、桶、冷却与随机状态 |
| P05 | 临近孵化和回弹阶段保存、加载 | 与不中断的相同输入运行结果一致，不重复孵化或击杀 |
| P06 | 多次保存 / 读取同一高击杀局 | 历史纪录不重复累加 |
| P07 | 两个异步保存先后完成次序颠倒 | 正式存档仍为保存序号较新的快照 |
| P08 | 主存档损坏，备份有效；两者均损坏 | 前者可恢复并提示；后者不覆盖原文件或静默重置 |
| P09 | 1280×720、1920×1080、2560×1440 和 21:9，三档 UI 缩放 | 核心信息和按钮不遮挡、不裁切，操作区域正确 |
| P10 | 同屏代表达到上限后连续电拍 | 只扣实际逻辑数量；死亡与 -10 可感知；对象数受控 |
| P11 | 清场与孵化同 Tick | 表现顺序正确，旧蚊消失、后代存在，HUD 跟随真实数量 |
| P12 | 开关震屏、降低闪光、嗡鸣调为 0 | 设置立即有效，不改变种子和逻辑结果 |
| P13 | 短卡顿、积欠过多、休眠唤醒 | 分别符合第 7.4 节，不用墙钟整段乘法产生额外后代 |
| P14 | 按第 14 章的数量与操作组合运行 30 分钟 | 输出帧耗时、Tick 耗时、内存、对象峰值与构建信息报告 |
| P15 | 性能暂停时仍有完整 Tick 积欠，保存退出后继续 | 积欠不丢失；先补算再接受输入，与不中断补算的状态一致 |
| P16 | 暂停状态下保存返回或正常退出 | 不推进 Tick 也能写入最新快照，失败不会被当作保存成功 |

### 16.4 试玩判定

首轮邀请约 5 位未参与开发的玩家，每人自由操作 5–10 分钟，记录行为并在结束后简短询问。以下是调整方向的观察门槛，不是已经取得的结论：

- 至少 4 人能解释“母蚊产卵，蚊香不杀蛋”。
- 至少 4 人能分辨手掌 MISS 与没有按到按钮。
- 至少 3 人在首次清场后自愿再次使用武器，且不会把正常回弹理解为数量 Bug。
- 记录首次蚊香、第二次蚊香、主动重开、首次闲置超过 30 秒以及退出的时间。
- 观察全武器解锁后是否仍有主动操作意愿；若多数人在两分钟内失去兴趣，先调整目标或操作反馈，不直接进入多地图制作。

## 17. 后续扩展与待验证事项

### 17.1 按验证结果选择扩展

| 观察到的问题 | 优先尝试 | 不应直接假定的答案 |
|---|---|---|
| 清场有趣，但很快无事可做 | 独立的 60 秒累计击杀挑战或数量区间挑战 | 立刻制作完整 RPG 成长树 |
| 玩家更想亲手瞄准 | 单独做鼠标区域命中原型，重新定义视觉与逻辑映射 | 只加碰撞器就能保留原全局随机规则 |
| 高数量看不出区别 | 数量层级、群体动画与声音设计 | 增加到数百万实体 |
| 清场后无蛋时体验失落 | 加强“已彻底清除”的结算奖励感 | 暗中生成蛋保证回弹 |
| 三把武器很快全解锁 | 重新验证阈值、冷却和首次体验 | 在提示动画期间偷偷减慢繁殖 |
| 玩家明确希望挂机 | 设计独立离线规则、收益上限及保存政策 | 直接按离线时长乘母蚊数量 |

新武器、地图、变种蚊子、自动攻击、永久成长及平台成就可以后续增加，但都要说明是否影响虫卵、如何改变终止条件、是否跨局保留。

### 17.2 开工与试玩中的待验证清单

| 时点 | 待验证内容 | 当前可用基线 |
|---|---|---|
| 工程创建 | Unity / Blender 的具体版本及脚本后端 | 使用受支持稳定版本并锁定，先完成样例构建 |
| M0 资产样例 | 画面风格、蚊子尺寸、材质与翅膀方案 | 风格化固定镜头，先制作一只 |
| M2 可玩原型 | 全局攻击是否有足够参与感 | 固定镜头、按钮选武器、点击场景使用 |
| 首次试玩 | 十几秒解锁全部武器是否过快 | 保留 10 / 100 / 1,000 阈值，按测量调整 |
| M4 性能验证 | 目标电脑与预算能否达标 | 第 14 章建议基准，尚未实测 |
| 后续发行 | 支持系统、渠道、定价、语言、手柄 | 当前仅 Windows x64 本地原型，不预先承诺其他范围 |

这些事项不阻止纯逻辑和灰盒原型推进，但正式美术量产、发行承诺和大范围功能扩展应以相应验证结果为依据。

## 18. 附录

### 附录 A：配置表

玩法配置只能在新局生效；已开始的局使用其快照。开发调试允许显式重置测试局后修改，不能在存在旧母蚊桶和虫卵桶时任意热改周期。视觉、音频和 UI 配置可以即时调整。

| Key | 基线 | 类型 / 生效方式 |
|---|---|---|
| simulationTickMs | 50 | 技术规则；随模拟版本锁定 |
| startMale / startFemale | 2 / 2 | 玩法规则；新局 |
| initialFirstLayTicks | 20 | 玩法规则；新局 |
| femaleLayIntervalTicks | 20 | 玩法规则；新局 |
| newFemaleFirstLayTicks | 20 | 玩法规则；新局 |
| eggHatchDelayTicks | 10 | 玩法规则；新局 |
| smallBatchSexLimit | 1,024 | 模拟算法参数；随版本锁定 |
| femaleProbability | 0.5 | 小批量性别概率；本版锁定 |
| largeBatchSexMode | HalfWithRandomOdd | 大批量算法；随版本锁定 |
| handUnlockCount / zapperUnlockCount / incenseUnlockCount | 10 / 100 / 1,000 | 玩法参数；新局 |
| handHitProbability | 0.5 | 玩法规则；新局 |
| handKillCount / zapperKillCount | 1 / 10 | 玩法规则；新局 |
| handCooldownTicks / zapperCooldownTicks / incenseCooldownTicks | 6 / 16 / 200 | 玩法参数；新局 |
| incenseAfterHatchWindowTicks | 10 | 表现状态时长；不改虫卵 deadline |
| maxVisualAdults | 300 | 显示参数；即时，可降至 200 / 100 |
| maxDeathRepresentatives | 30 | 显示预算；即时 |
| maxActiveParticles | 1,500 | 显示预算；即时 |
| hudRefreshHz | 10 | 显示参数；关键事件另外立即刷新 |
| maxTicksPerRenderFrame | 5 | 工程参数 |
| backlogPauseThresholdTicks | 20 | 工程参数；积欠超过此值进入保护暂停 |
| autoSaveIntervalTicks | 200 | 工程参数；每 10 个模拟秒 |
| allowOfflineProgress | false | 首版产品规则 |
| globalWeaponUnlocks | false | 首版产品规则；只保存本局解锁 |

### 附录 B：统计与调试面板

本局统计至少包括：模拟时长、累计击杀、按性别击杀、最大单次蚊香击杀、手掌合法尝试与命中、电拍使用、蚊香使用、累计产卵、累计孵化、按性别出生、成蚊/虫卵/母蚊峰值、里程碑。

工程统计包括：当前桶数、单 Tick 耗时、渲染帧耗时、活蚊与死亡代表数、粒子与音源数、GC 分配和停顿、内存、存档耗时、补算积欠。

调试面板提供固定种子、单步 Tick、显示到期桶、注入合法场景、导出命令日志及切换质量档位。调试局带显式标记，不写玩家正式纪录；正式构建默认不展示开发字段。

### 附录 C：核心伪代码

以下是规则对应关系，不是可直接复制编译的 Unity 脚本。`Commit` 在原子事务完成后维护缓存、峰值、解锁与统计；各事务中间态不对外作为最终快照。

```text
StartRun(config, seed):
    tick = 0
    phase = Running
    male = 2
    femaleBuckets = { 20: 2 }
    eggBuckets = {}
    readyAt = { Hand: 0, Zapper: 0, Incense: 0 }
    unlocked = {}
    runMaxAdult = 4
    initializeStatsAndVersionedRng(seed)

Step(command):
    if paused or phase == ReproductionEnded: return
    tick += 1

    // 攻击在本 Tick 产卵和孵化之前发生。
    if command exists:
        TryUseWeapon(command.weapon)

    laying = RemoveBucket(femaleBuckets, tick)
    if laying > 0:
        Commit:
            AddBucket(eggBuckets, tick + 10, laying)
            AddBucket(femaleBuckets, tick + 20, laying)
            totalEggsCreated += laying
    appendRecentLayHistory(laying)

    hatching = RemoveBucket(eggBuckets, tick)
    if hatching > 0:
        (boys, girls) = SplitSex(hatching)
        Commit:
            male += boys
            AddBucket(femaleBuckets, tick + 20, girls)
            totalHatched += hatching
            totalMaleBorn += boys
            totalFemaleBorn += girls
            if phase == HatchAfterClear:
                burstHatchedCount += hatching

    if femaleCount == 0 and eggCount == 0:
        phase = ReproductionEnded
    else if phase == HatchAfterClear and tick >= burstEndTick:
        phase = Running

    validateInvariants()
    publishOrderedEventsAndSnapshot()
    flushMergedSaveRequestAtTickBoundary()

OnSaveFromPausedMenuOrExit():
    // 主循环独立入口，不调用 Step，不推进模拟时钟。
    clearPendingAttackInputs()
    snapshot = captureLastCompletedTickAndDriverBacklog()
    SaveService.submitWithMonotonicSequence(snapshot)
    // 正常退出须等待最新提交完成，失败按第 13.2 节反馈。

TryUseWeapon(weapon):
    if paused or phase == ReproductionEnded: return
    if not unlocked[weapon] or adultCount == 0: return
    if tick < readyAt[weapon]: return
    if weapon == Incense and phase == HatchAfterClear: return

    readyAt[weapon] = tick + configuredCooldownTicks(weapon)

    if weapon == Hand:
        handAttempts += 1
        if nextLogicRandomBool50() == false:
            emitMiss()
            return
        handHits += 1
        KillUniformAdultsWithoutReplacement(1)

    else if weapon == Zapper:
        zapperUses += 1
        KillUniformAdultsWithoutReplacement(min(10, adultCount))

    else:
        killedMale = male
        killedFemale = femaleCount
        survivingEggs = eggCount
        Commit:
            male = 0
            clear(femaleBuckets)
            recordKills(killedMale, killedFemale)
            recordIncenseUse(killedMale + killedFemale, survivingEggs)
        // eggBuckets 完全不动。
        if survivingEggs > 0:
            phase = HatchAfterClear
            burstEndTick = tick + 10
            burstInitialEggCount = survivingEggs
            burstHatchedCount = 0
        requestSaveAfterTick()

SplitSex(n):
    if n <= 1024:
        girls = numberOfTrueResultsFromNLogicRandomBool50(n)
    else:
        girls = floor(n / 2)
        if n is odd and nextLogicRandomBool50(): girls += 1
    return (n - girls, girls)
```

`KillUniformAdultsWithoutReplacement` 每次最多循环 10 次：按第 10.4 节的固定区间顺序，使用同一个随机整数确定成蚊性别及母蚊桶；然后提交桶、性别计数和击杀统计。每次抽取都用扣除后的群体更新概率。

实际代码读取配置中的 Tick 值，不在多处硬编码 10 / 20 / 200。伪代码固定值仅用于展示本版基线。

### 附录 D：参考资料与证据边界

- 原始设计来源：《蚊子游戏模拟器_游戏策划文档_GDD_v1.0.md》（历史参考，当前仓库未收录）。
- [Unity：渲染管线概述](https://docs.unity.com/en-us/engine/6000.0/manual/render-pipelines/overview)——用于 URP 选型背景，具体版本与项目性能仍需验证。
- [Unity：模型导入](https://docs.unity3d.com/6000.0/Documentation/Manual/ImportingModelFiles.html)——支持采用明确的 FBX 导出交付流程。
- [Microsoft：C# 浮点数类型范围](https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/builtin-types/floating-point-numeric-types)——说明 decimal 并非任意范围整数。
- [Microsoft：System.Numerics.BigInteger](https://learn.microsoft.com/en-us/dotnet/api/system.numerics.biginteger)——整数类型参考；具体 Unity 运行环境仍需构建验证。

第 8.1 节为前次规则脚本的测算记录；第 14 章为拟定性能目标；第 16.4 节为拟定试玩观察门槛。本文没有声称已经完成 Unity 项目、运行性能测试或真人试玩。

---

**文档结束 · v1.1 · PC / Unity / Blender 原型建议基线**
